Project protalFornt = project(":qartal-portal-frontend")

ext {
  // Spring build dir
  bootJarDir = "BOOT-INF/classes/static"

  // GWT build dir
  buildDirGwt = "${protalFornt.buildDir}/gwt"
  extraDirGwt = "${protalFornt.buildDir}/extra"
}